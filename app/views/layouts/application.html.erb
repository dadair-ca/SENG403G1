<!DOCTYPE html>
<html>
<head>
  <title>SENG403G1</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= stylesheet_link_tag    "application", "http://fonts.googleapis.com/css?family=Oswald", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div id="container">
	<div id="header">
		<%= link_to "", root_path, :class => "logo" %>
		<div class="header-mid">
			<%= link_to root_path, :class => "weblib" do %>
				Web<span class="weblib-right">Lib</span>
			<% end %>
			<% if request.path_parameters[:controller] == "home" %>
				<div class="welcome">
					Welcome to WebLib, a library management system!
				</div>
			<% else %>
				<div class="navigation">
					<%= link_to "Search Catalog", items_search_path %>
					| <%= link_to "Browse Catalog", items_path %>
					| <%= link_to "Browse Authors", authors_path %>
				<% if user_signed_in? and current_user.category > 0 %>
					| <%= link_to "Browse Rentals", rentals_path %>
				<% end %>
				</div>
			<% end %>
		</div>
		<% if request.path_parameters[:controller] != "home" %>
		<div class="header-right">
      <% if user_signed_in? %>
        Welcome <strong><%= current_user.given_name %> <%= current_user.surname %></strong> | <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
      <% else %>
        <%= link_to('Login', new_user_session_path) %>
      <% end %>
		</div>
		<% end %>
	</div>

	<div id="content">
		<div class="wrapper">
			<%= yield %>
		</div>
	</div>
	
	<div id="footer">
		<div class="wrapper">
			<p>Search Catalog | Browse Genre | Admin Panel</p>
			<p>Created by Infomatics Team (David Adair, Shena Fortozo, Naomi Hiebert, Gellert Kispal, Sydney Pratte, Ho Wai Yung)</p>
			<p>SENG 403, Winter 2013</p>
		</div>
	</div>
</div>

</body>
</html>
