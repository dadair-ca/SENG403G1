<%= form_for [@item, @copy] do |f| %>
  <% if @copy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@copy.errors.count, "error") %> prohibited this copy from being saved:</h2>
      <ul>
      <% @copy.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :barcode_id %><br />
    <%= f.text_field :barcode_id %><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
